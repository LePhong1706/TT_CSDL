@model Web_vuottai.Models.CAKIP

@{
    ViewData["Title"] = "Edit";
}

<h1>Chỉnh sửa thông tin ca kíp</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CaKipId" />
            <div class="form-group">
                <label asp-for="TenCaKip" class="control-label">Tên ca kíp</label>
                <input asp-for="TenCaKip" class="form-control" />
                <span asp-validation-for="TenCaKip" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HeSo" class="control-label">Hệ số</label>
                <input asp-for="HeSo" class="form-control" />
                <span asp-validation-for="HeSo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Lưu" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
